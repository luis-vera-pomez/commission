%h1 User

.row
  .col-sm-4
    %p#notice= notice
    %p
      %strong First name:
      = @user.first_name
    %p
      %strong Last name:
      = @user.last_name
    %p
      %strong Email:
      = @user.email
    %p
      %strong Archived:
      = @user.archived

  .col-sm-4.offset-1
    = simple_form_for [:admin, @user] do |f|
      = f.input :roles, collection: EffectiveRoles.roles_collection(f.object), as: :check_boxes, disabled: true

%hr

= link_to 'Edit', edit_admin_user_path(@user)
|
= link_to 'Back', admin_users_path