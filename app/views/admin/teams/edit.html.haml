%h1 Editing Team
= render 'form', user: @team

%h2 Agents List

.row
  .col-sm-12.text-right.action-buttons
    = link_to new_admin_team_agent_path(@team), class: 'btn btn-primary' do
      New Agent

= render_datatable @agents

%hr

%h2 Sales List

= render_datatable @sales

%hr

= link_to 'Show', admin_team_path(@team)
|
= link_to 'Back', admin_teams_path